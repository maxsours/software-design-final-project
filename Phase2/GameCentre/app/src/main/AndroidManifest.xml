<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fall2018.csc2017.slidingtiles">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.DarkActionBar">
        <activity android:name=".StartingActivity">
        </activity>
        <activity android:name=".GameActivity" />
        <activity
            android:name=".ScoreActivity"
            android:label="@string/title_activity_score" />
        <activity
            android:name=".GameModeSelection"
            android:theme="@style/AppTheme.TranslucentCloseOnTouch" />
        <activity android:name=".ActualStartingActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".draughts.SettingsActivity" />
        <activity android:name=".draughts.MyCheckersActivity" />
        <activity android:name=".Game2048.MainActivity" />

    </application>

</manifest>